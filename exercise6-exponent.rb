# Write an exponent(num, power) function that takes two arguments (a number and an exponent value) and returns the computed result. Below are a few example inputs. Make sure you return your result instead of just printing it!

def exponent(num,power)
    return num**power
end

puts exponent(10, 0)
puts exponent(5, 3)
puts exponent(2, 10)